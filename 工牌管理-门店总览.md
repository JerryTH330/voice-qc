# 工牌管理：工牌总览

> 阅读本文件前，先读取 [工牌管理-通用规则.md](工牌管理-通用规则.md)。涉及接口能力或数据缺口时，再读取 [工牌管理-待确认事项.md](工牌管理-待确认事项.md)。

## 1. 页面目标

按查询日期、品牌和组织查看存在工牌绑定关系的门店，以及门店员工和工牌当前有效绑定情况。

## 2. 页面数据范围

- 页面只展示“当前有效绑定数大于等于 1”的门店。
- 当前有效绑定数为 0 的门店不进入列表，也不计入门店数。
- 品牌和组织筛选后的指标卡、列表总数和分页必须基于同一批门店数据计算。
- 工牌绑定数据使用循环智能返回的 `staff_id（= 顾问 ID）`，关联质检系统顾问主数据后取得门店、战区、大区和品牌，并按门店汇总。

## 3. 筛选条件

| 筛选项 | 数据来源 | 规则 |
| --- | --- | --- |
| 查询日期 | 设备及绑定数据 | 默认查询昨天；默认开始日期和结束日期均为昨天，形成单日查询；需要时可选择日期范围 |
| 品牌 | 当前质检系统 | 默认全部品牌 |
| 组织 | 当前质检系统 | 支持大区、战区、门店三级逐级筛选；选择上级时包含全部下级门店 |
| 门店名称 | 当前质检系统 | 支持输入门店名称关键字快捷筛选；输入后即时更新指标卡、列表总数和分页 |

查询日期以用户所在时区的自然日计算。门店名称与查询日期、品牌、组织条件同时生效，按门店名称包含关键字进行匹配，输入内容首尾空格不参与匹配。重置筛选后，查询日期恢复为昨天，品牌恢复全部品牌，组织恢复全部组织，门店名称清空，并返回第 1 页。

## 4. 指标卡

工牌总览按以下固定顺序展示 6 个指标：

| 顺序 | 指标 | 图标字 | 计算规则 |
| --- | --- | --- | --- |
| 1 | 门店数 | 店 | 当前筛选范围内，当前有效绑定数大于等于 1 的门店去重数；指标卡增加“仅统计使用工牌的门店数”说明 |
| 2 | 工牌资产数 | 牌 | 当前筛选范围内各门店有效工牌资产数之和，包含已绑定和未绑定工牌；数据来源待确认 |
| 3 | 工牌绑定数 | 绑 | 当前有效绑定关系的记录总数 |
| 4 | 工牌绑定率 | 绑 | 当前筛选范围内的工牌绑定数 ÷ 工牌资产数 × 100%，按汇总值计算，不取各门店绑定率的平均值 |
| 5 | 员工数 | 人 | 上述门店内，当前质检系统已同步的员工去重数 |
| 6 | 绑定工牌员工数 | 绑 | 当前有效绑定关系中的顾问 ID 去重数 |

原口径不清晰的“工牌数”仍不展示。页面统一展示名称“工牌资产数”，业务含义仍是当前筛选范围内的门店工牌资产数。

## 5. 门店列表

列表字段固定为：

1. 品牌
2. 大区
3. 战区
4. 门店名称
5. 工牌资产数
6. 工牌绑定数
7. 工牌绑定率
8. 员工数
9. 绑定工牌员工数
10. 最新数据同步时间
11. 操作

字段规则：

- 员工数：使用当前质检系统人员数据，按顾问 ID 去重。
- 工牌资产数：统计归属于该门店且处于有效资产状态的工牌 SN 去重数，包含已绑定和未绑定工牌；报废、退役和已调出该门店的工牌不统计。
- 绑定工牌员工数：使用当前有效绑定关系，按 `staff_id（= 顾问 ID）` 去重。
- 工牌绑定数：使用当前有效绑定关系记录总数。
- 工牌绑定率：`工牌绑定数 ÷ 工牌资产数 × 100%`，保留 1 位小数。工牌资产数为 0 或数据缺失时显示“—”，不能按 0% 展示。
- 工牌资产数、工牌绑定数、工牌绑定率、员工数、绑定工牌员工数均支持排序；首次点击按降序排列，再次点击切换为升序，数据缺失的门店固定排在最后。
- 工牌绑定率超过 100% 时不截断为 100%，应原值展示并标记为资产或绑定关系数据异常。
- 最新数据同步时间：取该门店当前有效绑定工牌中，设备最近同步时间的最大值；数据来源优先级按通用规则执行。
- 查看明细：进入工牌明细，并自动带入当前门店筛选条件。

## 6. 分页

- 分页对象是符合当前筛选且当前有效绑定数大于等于 1 的门店。
- 支持每页 10、20、50 条、上一页、下一页和跳页。
- 筛选条件发生变化后返回第 1 页。

## 7. 页面验收重点

1. 当前有效绑定数为 0 的门店不出现在工牌总览，也不计入门店数。
2. 页面使用“工牌资产数”，不再使用定义不清晰的“工牌数”。
3. 工牌资产数包含归属门店的未绑定有效工牌，且数据来源已经确认。
4. 工牌绑定率按“工牌绑定数 ÷ 工牌资产数”计算，汇总指标不使用门店绑定率平均值。
5. 工牌资产数、工牌绑定数、工牌绑定率、员工数和绑定工牌员工数均支持降序、升序排序，缺失值排在最后。
6. 查询日期默认昨天，重置筛选后也恢复为昨天。
7. 指标卡与列表数量字段按本文件规定的顺序展示，指标卡图标字分别为“店、牌、绑、绑、人、绑”。
8. 输入门店名称关键字后即时筛选，指标卡、列表总数和分页使用同一筛选结果。
9. 门店、战区、大区和品牌必须通过绑定关系中的 `staff_id` 关联质检系统顾问主数据取得；`staff_id` 无法匹配时不进入正常门店汇总，并记录数据异常。
