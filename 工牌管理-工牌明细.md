# 工牌管理：工牌明细

> 阅读本文件前，先读取 [工牌管理-通用规则.md](工牌管理-通用规则.md)。涉及接口能力或数据缺口时，再读取 [工牌管理-待确认事项.md](工牌管理-待确认事项.md)。

## 1. 页面目标

查看每条当前有效“顾问—工牌”关系对应的组织、人员和设备实时状态，并支持进入工牌事件和上传日志。

## 2. 页面数据范围

- 一行代表一条当前有效绑定关系。
- 未绑定员工或没有当前有效绑定关系的工牌不进入本页。
- 页面不展示独立指标卡。
- “当前匹配、已连接、录音中”保留在列表标题栏，并随筛选结果即时更新；当前匹配数量与列表总数、分页总数必须一致。
- 列表标题栏不重复展示查询日期，查询范围以上方筛选区为准。

## 3. 筛选条件

| 筛选项 | 数据来源 | 规则 |
| --- | --- | --- |
| 品牌 | 当前质检系统 | 默认全部品牌 |
| 组织 | 当前质检系统 | 支持大区、战区、门店、销售顾问四级筛选 |
| 查询日期 | 设备及绑定数据 | 使用与录音列表一致的日期范围组件；默认开始日期和结束日期均为昨天 |
| 工牌 SN | 当前有效绑定关系 | 支持输入完整或部分 SN 快捷筛选 |
| 门店名称 | 当前质检系统 | 支持输入门店名称关键字快捷筛选，输入后即时更新列表标题栏汇总、列表和分页 |
| 顾问 ID | 当前质检系统 | 支持 ID 模糊查询 |
| 顾问姓名 | 当前质检系统 | 支持姓名模糊查询 |

筛选区第一行固定为“品牌、组织、查询日期”，第二行固定为“工牌 SN、门店名称、顾问 ID、顾问姓名、操作”。所有筛选条件同时生效。门店名称按包含关键字进行匹配，输入内容首尾空格不参与匹配。重置后清空门店名称等快捷查询条件，恢复默认值并返回第 1 页。

## 4. 工牌列表

列表字段固定为：

1. 工牌 SN
2. 品牌
3. 大区
4. 战区
5. 门店
6. 顾问 ID
7. 顾问姓名
8. 录音状态
9. WiFi 连接
10. 是否接入充电坞
11. 信号
12. 剩余电量
13. 低电量预警
14. 剩余内存
15. 今日开机时长
16. 待上传录音
17. 最新数据同步时间
18. 操作

## 5. 列表字段规则

### 5.1 工牌 SN

使用当前有效绑定关系中的工牌 SN，是设备状态查询、事件查询和上传日志查询的关联键。

### 5.2 录音状态

- 明略 `record_status=1`：录音中。
- 除“录音中”外，其他情况均无法检测具体状态，统一显示“—”。
- “录音中”使用绿色状态展示。

### 5.3 WiFi 连接

- 字段值只展示“已连接”或“未连接”。
- 不在该字段中混入网络类型或信号等级。

### 5.4 是否接入充电坞

- 当前使用循环 `is_connected`。
- `true` 显示“已接入”，`false` 显示“未接入”。
- 明略 `usb_status` 是否可等同于“接入充电坞”尚未确认，确认前不得替换或合并该口径。

### 5.5 信号

- 信号强度使用明略 `rssi`。
- 页面只转换为“信号良好”“一般”“较弱”三个等级。
- 页面不展示 WiFi/4G 网络类型。

### 5.6 剩余电量

- 优先使用明略 `remain_power`，取值范围 0—100。
- 页面只展示电量原始值，所有电量值均使用普通文字颜色，电量进度条也不因数值高低切换为红色。
- 不再使用“剩余电量低于 20%”判断低电量预警。
- 明略值缺失时可使用循环 `battery`。

### 5.7 低电量预警

- 数据只取明略操作日志。
- 当前查询日期范围内，只要同一工牌出现 `ops=system`、`act=battery low` 的日志，显示“有预警”；没有对应日志显示“—”。
- 低电量预警不能通过 `remain_power` 数值计算或补推，即使剩余电量低于 20%，没有指定操作日志也不显示预警。

### 5.8 剩余内存

页面字段名称统一为“剩余内存”，统一展示百分比，不得显示为“存储使用率”。

- 明略：`剩余内存百分比 = 100% - 已使用百分比`。
- 明略有剩余空间和总空间时，等价计算为：`remain_storage ÷ total_storage × 100%`。
- 循环：`memory` 的定义本身就是剩余内存百分比，可直接展示，不能再用 `100-memory` 二次转换。
- 计算结果按页面统一规则取整，并限制在 0%—100%。
- 剩余内存低于 20% 时，数值使用红色预警；20% 及以上按正常状态展示。

### 5.9 今日开机时长

- 优先根据明略当日操作日志中的开机、关机事件计算。
- 跨日仍未关机时，从当日 00:00 开始计算。
- 当日最后一次开机后仍在线时，计算至状态查询时间。
- 日志缺失或事件无法配对时显示“—”，并保留异常标记供排查。

### 5.10 待上传录音

- 数量使用明略 `recnu`。
- 明细通过 `reclist` 获取。
- `recnu=0` 显示 0；字段缺失显示“—”。

### 5.11 最新数据同步时间

- 数据来源优先级按通用规则执行。
- 页面按通用规则中的时间格式展示。

### 5.12 操作

- 工牌事件：按工牌 SN 查看开关机、录音、充电和低电量等事件。
- 上传日志：按工牌 SN 查看录音待上传及上传完成记录。

## 6. 分页

- 分页对象是当前筛选后的有效绑定关系。
- 支持每页 10、20、50 条、上一页、下一页和跳页。
- 筛选条件发生变化后返回第 1 页。

## 7. 页面验收重点

1. WiFi 连接只允许“已连接”“未连接”两个值。
2. 列表必须展示“是否接入充电坞”。
3. 页面统一展示“剩余内存”，不得显示“存储使用率”。
4. 信号只允许“信号良好”“一般”“较弱”三个值，不展示 WiFi/4G 网络类型。
5. 录音状态只允许“录音中”或“—”，“录音中”使用绿色展示。
6. 页面不展示独立指标卡；列表标题栏保留“当前匹配、已连接、录音中”汇总。
7. 输入门店名称关键字后即时筛选，列表标题栏汇总、列表和分页使用同一筛选结果。
8. 筛选顺序固定为：第一行“品牌、组织、查询日期”，第二行“工牌 SN、门店名称、顾问 ID、顾问姓名、操作”。
9. 列表字段统一命名为“剩余电量”，不得显示为“电量”。
10. 剩余电量不按数值设置文字或进度条颜色，也不得用低于 20% 推断低电量预警。
11. 列表标题栏不展示查询日期，查询范围仅在上方筛选区展示。
12. 列表必须在“剩余电量”后展示“低电量预警”；只有明略操作日志出现 `ops=system`、`act=battery low` 时显示“有预警”，否则显示“—”。
13. 剩余内存低于 20% 时仍使用红色预警，20% 不触发预警。
