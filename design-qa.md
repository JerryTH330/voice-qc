# 厂端录音复盘规则与组织列表设计验收

- 源视觉依据：Figma `5nY2kC6LvaldZ85UuxjF28` 节点 `565:5402`。
- 源视觉路径：`https://www.figma.com/design/5nY2kC6LvaldZ85UuxjF28/Untitled?node-id=565-5402`
- 组织列表依据：Figma 节点 `566:7129`，路径 `https://www.figma.com/design/5nY2kC6LvaldZ85UuxjF28/Untitled?node-id=566-7129`。
- 下钻指标卡依据：Figma 组合节点 `566:7058`；命中率卡 `566:7059`，命中/样本卡 `566:7081`。
- 源图像尺寸：696 × 611 px；节点尺寸 654 × 563 CSS px。
- 实现页面：`factory-dashboard/index.html?route=factory-dashboard`。
- 实现截图路径：无；内置浏览器安全策略禁止刷新和读取本地 `file://` 页面。
- 验收视口：用户当前 1567 × 990 CSS px；无实现截图，无法进行密度归一化。
- 页面状态：厂端看板 / SOP 质检分析 / 规则列表首页。

## Findings

- [P2] 缺少修改后浏览器渲染证据
  - 证据：已获取 Figma 截图和完整节点数据，但本地 `file://` 页面被 URL 安全策略拦截。
  - 影响：无法产出与 Figma 同视口的实现截图，不能宣称视觉对比已通过。
  - 处理：已用回归测试锁定 654px 外框上限、312/70/98/92 四列、40px 行高、12px 行间距、标签 30×22px 和设计稿颜色；表头水平内距修正为 14px，最后一列标题和操作链接统一从网格列左侧起点排列。

## Required Fidelity Surfaces

- 字体与排版：对齐 14px、Semibold、21px 表头行高和 20/21px 内容行高；中文使用系统 PingFang SC 回退。
- 间距与布局：外框 13px 内距、12px 内部间距、10px 列间距；列宽 312/70/98/92px，宽度不足时仅收缩首列；表头用 14px 水平内距补偿数据行边框，最后一列标题与“看组织表现”共享同一左起点。
- 组织列表：654px 外框上限、406/48/76/42px 四列、13px 内距、12px 行间距、40px 最小行高和 9px/13px 行内距；按用户最新要求，“操作/下钻”统一从最后一列左侧起点排列，可下钻项增加右箭头。
- 下钻指标卡：整组 654×80px、双列各 321px、间距 12px；单卡 13/17/13/13px 内距、14px 圆角、1px 蓝色 14% 描边、`rgba(248,251,255,0.49)` 到 `rgba(241,246,255,0.46)` 浅蓝灰渐变和 4px 背景模糊；图标 48px，标题 14px、数值 24px。
- 颜色与视觉变量：表头 `#8a96ab`，内容 `#162033`，操作 `#2563eb`，标签 `#215fd0` / `#d3dff6`。
- 图像与图标：新节点无新图像资产；保留用户上一轮明确要求的操作右箭头。
- 文案与内容：表头和规则文案与 Figma 一致；场景标签保留首字及完整语义。

## Comparison History

- 用户再次反馈“操作”仍未对齐。复现确认原因不是整行间距，而是固定 70px 的标题与自适应宽度的操作链接分别右对齐，导致左边缘不同；现已改为共同从最后一列左侧起点排列。由于浏览器禁止读取 `file://` 页面，仍无法生成修改后截图并完成可见比较。
- 组织列表已按 `566:7129` 的结构化设计数据和独立截图完成数值对齐；随后根据用户最新标注明确覆盖设计稿，为“下钻”补回右箭头，并将“操作”和“下钻”统一左对齐。浏览器限制仍阻止修改后页面截图。
- 两张下钻指标卡已按组合节点 `566:7058` 完成尺寸和样式对齐，并分别接入 Figma 提供的两个原始 PNG 图标；浏览器限制仍阻止修改后页面截图。
- 再次读取最新 `566:7059` 后确认背景已从白色半透明渐变更新为浅蓝灰渐变；描边仍为 1px `rgba(37,99,235,0.14)`，已通过专项测试锁定。

## Implementation Checklist

- [x] 外框、四列宽度、行高与间距按 `565:5402` 实现；标题实际起点与数据行一致。
- [x] 字号、字重、文字颜色和标签颜色按节点实现。
- [x] 保留搜索、排序、分页、下钻、首字标签和右箭头。
- [x] 组织列表外框、四列、表头、行高、字重和颜色按 `566:7129` 实现，保留下钻交互。
- [x] 下钻指标卡双列布局、卡片视觉、文字规格和两个独立图标按 `566:7058` 实现。
- [x] 专项测试、JavaScript 语法和差异检查通过。
- [ ] 用户刷新本地页面后完成同视口视觉复核。

## Follow-up Polish

- 如刷新后发现中文字体或 1px 边界存在渲染差异，再根据实际截图微调。

final result: blocked
