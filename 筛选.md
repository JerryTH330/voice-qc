# 列表筛选业务规则

> 版本：V1.0
> 更新日期：2026-09-03
> 适用范围：录音列表及其他采用相同组织、地域、门店、人员条件的列表页面

## 一、筛选项

范围筛选包括：

- 品牌
- 大区
- 战区
- 省份
- 城市
- 门店
- 巡回员
- 治理员
- 销售顾问

人员查询支持：

- 顾问 ID
- 顾问姓名
- 巡回员姓名
- 治理员姓名

## 二、数据关系

### 1. 组织关系

~~~text
品牌 → 大区 → 战区 → 门店
~~~

- 一个门店只归属于一个品牌、一个大区、一个战区。
- 品牌、大区、战区之间是明确的上下级关系。

### 2. 地理关系

~~~text
省份 → 城市 → 门店
~~~

- 一个门店只归属于一个省份、一个城市。
- 省份、城市之间是明确的上下级关系。

### 3. 人员与门店关系

- 一个巡回员可以负责多家门店，一家门店在同一时间只对应一名巡回员。
- 一个治理员可以负责多家门店，一家门店在同一时间只对应一名治理员。
- 一家门店可以有多名销售顾问。
- 人员与门店的关系按数据发生时间判断。

## 三、总原则

### 原则一：明确上下级关系才做层级联动

适用于：

- 品牌 → 大区 → 战区 → 门店
- 省份 → 城市 → 门店

上级限制下级候选范围；上级变化时，可以清除与上级冲突的下级条件。

### 原则二：直接归属关系才缩小候选范围

适用于：

- 巡回员 ↔ 门店
- 治理员 ↔ 门店
- 销售顾问 ↔ 门店

人员与门店可以双向缩小候选范围，但不自动替用户选中条件。

### 原则三：其他维度不互相限制候选

没有明确上下级或直接归属关系的字段，不互相缩小下拉候选、不自动填写、不自动清除。

适用于：

- 大区 ↔ 省份
- 大区 ↔ 城市
- 战区 ↔ 省份
- 战区 ↔ 城市
- 巡回员/治理员 ↔ 大区/战区
- 巡回员/治理员 ↔ 省份/城市
- 巡回员 ↔ 治理员

这些条件只在最终列表查询时按 AND 取交集。

### 原则四：同字段 OR，不同字段 AND

- 同一个筛选项多选时采用 OR。
- 不同筛选项之间采用 AND。

例如：

~~~text
巡回员 = 张三、李四
城市 = 广州
~~~

最终逻辑为：

~~~text
（张三 OR 李四）AND 广州
~~~

### 原则五：所有关系判断必须使用 ID

- 名称只用于展示和搜索。
- 品牌、大区、战区、门店、人员均使用唯一 ID 查询和判断关系。
- 同名组织或同名人员不能因为名称相同被当成同一个对象。

## 四、候选项联动矩阵

| 当前打开的筛选项 | 候选范围依据 | 不受哪些条件影响 |
|---|---|---|
| 品牌 | 当前用户的数据权限 | 大区、战区、省份、城市、人员 |
| 大区 | 当前用户权限；已选品牌 | 战区、省份、城市、人员 |
| 战区 | 当前用户权限；已选品牌、大区 | 省份、城市、人员 |
| 省份 | 当前用户的数据权限 | 品牌、大区、战区、人员 |
| 城市 | 当前用户权限；已选省份 | 品牌、大区、战区、人员 |
| 门店 | 已选组织、地域和人员条件共同形成的有效门店范围 | — |
| 巡回员 | 已选门店；未选门店时按权限和时间范围展示 | 大区、战区、省份、城市、治理员 |
| 治理员 | 已选门店；未选门店时按权限和时间范围展示 | 大区、战区、省份、城市、巡回员 |
| 销售顾问 | 已选门店；未选门店时按权限和时间范围展示 | 巡回员、治理员等非直接条件 |

说明：

- 门店是各条件最终落到具体数据范围时的承接对象。
- 门店候选可以同时受到组织、地域和人员条件限制。
- 不能因为门店是共同对象，就让组织字段与地理字段互相限制候选。

## 五、详细交互规则

### 规则 1：任意筛选项都可以作为第一个条件

用户不需要严格按照品牌、大区、战区等顺序选择。

用户可以直接选择：

- 门店
- 城市
- 巡回员
- 治理员
- 销售顾问

### 规则 2：上级向下级联动

选择品牌后：

- 大区只展示该品牌下的大区。
- 战区只展示该品牌下的战区。
- 门店只展示该品牌下的门店。

选择大区后：

- 战区只展示已选品牌、大区下的战区。
- 门店只展示已选大区下的门店。

选择省份后：

- 城市只展示该省份下的城市。
- 门店只展示该省份下的门店。

### 规则 3：选择下级不自动填写上级

例如用户直接选择“华南一战区”：

- 不自动填写品牌。
- 不自动填写大区。
- 战区条件正常参与最终查询。

例如用户直接选择“广州市”：

- 不自动填写广东省。
- 城市条件正常参与最终查询。

### 规则 4：上级可以重新选择

下级条件不能锁死上级候选范围。

例如已经选择：

~~~text
大区 = 华南大区
战区 = 华南一战区
~~~

用户重新打开大区时，仍可选择其他有权限的大区。

### 规则 5：上级变化只清除冲突的下级

例如用户将大区从华南大区改为东北大区：

- 清除不属于东北大区的战区。
- 清除不属于东北大区的门店。
- 不清除省份、城市、巡回员、治理员等跨维度条件。

多选时只清除失效的下级选项，继续保留仍然有效的选项。

清除后提示：

> 已清除与当前上级不匹配的筛选条件。

### 规则 6：组织与地域候选互不联动

选择大区或战区后：

- 不缩小省份候选。
- 不缩小城市候选。

选择省份或城市后：

- 不缩小大区候选。
- 不缩小战区候选。

例如：

~~~text
战区 = 华南一战区
省份 = 山东省
~~~

两个条件都保留，最终列表查询：

~~~text
华南一战区 AND 山东省
~~~

没有共同数据时显示 0 条。

### 规则 7：门店承接所有已选条件的交集

门店候选可以同时受到以下条件限制：

- 品牌、大区、战区
- 省份、城市
- 巡回员、治理员、销售顾问

例如：

~~~text
大区 = 华南大区
城市 = 广州
巡回员 = 张三
~~~

门店候选展示：

~~~text
华南大区门店 AND 广州门店 AND 张三负责门店
~~~

但大区、城市、巡回员三个筛选项之间不互相限制候选。

### 规则 8：巡回员、治理员与门店双向联动

选择巡回员后：

- 门店候选只显示该巡回员负责的门店。
- 不自动选中这些门店。

选择治理员后：

- 门店候选只显示该治理员负责的门店。
- 不自动选中这些门店。

选择门店后：

- 巡回员候选只显示所选门店对应的巡回员。
- 治理员候选只显示所选门店对应的治理员。
- 不自动选中人员。

### 规则 9：人员与组织、地域不联动候选

例如：

~~~text
巡回员 = 张三
城市 = 广州
~~~

- 城市候选不因为张三而缩小。
- 巡回员候选不因为广州而缩小。
- 最终列表只展示张三在广州负责门店的数据。

如果张三在广州没有负责门店，显示 0 条，条件继续保留。

### 规则 10：巡回员与治理员不互相限制候选

同时选择巡回员和治理员时：

- 两个人员条件都保留。
- 不自动修改对方的候选范围。
- 最终按照双方负责门店取交集。

没有共同门店时显示 0 条。

### 规则 11：销售顾问只查询本人数据

销售顾问条件不能只转换成门店范围。

例如王五和赵六都属于 A 店，选择：

~~~text
销售顾问 = 王五
~~~

最终查询必须同时保留：

~~~text
顾问 ID = 王五的顾问 ID
~~~

结果只展示王五本人的数据，不能展示赵六的数据或 A 店全部数据。

### 规则 12：删除条件后重新计算

删除任意条件后：

- 重新计算门店候选。
- 重新计算最终列表结果。
- 不自动修改其他仍然有效的条件。

### 规则 13：“全部”表示该字段不参与筛选

“全部”不是业务值，不应作为具体 ID 传给后台。

例如：

~~~text
品牌 = 传祺
大区 = 全部
城市 = 广州
~~~

最终条件为：

~~~text
传祺 AND 广州
~~~

## 六、多选规则

### 1. 同字段多选

同一字段的多个选项之间使用 OR。

例如：

~~~text
城市 = 广州、佛山
~~~

表示：

~~~text
广州 OR 佛山
~~~

### 2. 重新打开同一个多选下拉

计算候选项时：

- 忽略当前字段自身的已选值。
- 保留该字段的明确上级条件或直接关联条件。

例如没有其他条件时，选择广州后再次打开城市下拉，仍可继续选择佛山。

例如已经选择广东省时，城市只能继续多选广东省内城市，不能选择山东省内城市。

### 3. 部分选项没有结果

例如：

~~~text
巡回员 = 张三、李四
城市 = 广州
~~~

如果只有张三在广州有负责门店：

- 最终结果只展示张三的数据。
- 李四继续保留在已选条件中。
- 不自动清除李四。

## 七、人员输入查询规则

### 1. 输入查询与下拉使用同一个条件

顾问 ID、顾问姓名、巡回员姓名、治理员姓名输入框用于查找并选择人员。

选中人员后，应加入对应的人员筛选条件，不能形成另一套独立条件。

推荐交互：

> 可输入搜索的多选下拉框

既支持直接打开下拉选择，也支持输入姓名或 ID 查找。

### 2. 同名人员处理

遇到同名人员时，不能直接合并查询。

销售顾问搜索结果建议展示：

~~~text
张三｜顾问 ID：A001｜广州天河店
张三｜顾问 ID：A023｜佛山南海店
~~~

巡回员、治理员搜索结果建议展示：

~~~text
张三｜负责广州天河店等 3 家门店
张三｜负责青岛崂山店等 2 家门店
~~~

用户选择具体人员后，系统内部使用人员 ID 查询。

### 3. 搜索范围

- 已选择门店时，只搜索所选门店对应的人员。
- 未选择门店时，按用户数据权限和查询时间范围搜索。
- 大区、战区、省份、城市不缩小人员搜索候选。

## 八、历史关系规则

人员与门店的关系按数据发生时间判断，不能直接套用当前负责人。

例如：

- 7 月 A 店由张三负责。
- 8 月 A 店改由李四负责。

查询 7 月数据并选择张三时，应查出 A 店在 7 月的数据。

选择 A 店时：

- 查询时间仅覆盖 7 月，巡回员候选显示张三。
- 查询时间仅覆盖 8 月，巡回员候选显示李四。
- 查询时间同时覆盖 7 月和 8 月，候选可以同时显示张三和李四，并标明负责期间。

## 九、无结果及异常体验

### 1. 无共同数据

跨维度条件允许形成 0 条结果。

页面应：

- 保留全部已选条件。
- 不自动清除跨维度条件。
- 展示明确提示。

建议文案：

> 当前筛选条件没有共同数据，请调整筛选条件后重试。

### 2. 部分人员没有贡献结果

多选人员中只有部分人员有结果时，不自动清除无结果人员。

结果区可提示各人员匹配数量，例如：

~~~text
张三：4 条
李四：0 条
~~~

### 3. 重置筛选

重置后：

- 清空所有已选条件。
- 所有筛选项恢复“全部”。
- 时间条件恢复页面默认时间范围。
- 列表恢复默认结果和第一页。

## 十、权限与展示规则

- 所有候选项必须先受当前用户数据权限限制。
- 无权限的品牌、组织、门店和人员不能出现在下拉或搜索结果中。
- 同名大区、战区需要展示品牌和上级组织帮助区分。
- 建议展示格式：

~~~text
传祺｜华东大区
埃安｜华东大区
传祺｜华东大区｜华东一战区
~~~

- 已选条件应完整展示，并支持逐项删除。

## 十一、验收规则

1. 任意筛选项均可作为第一个条件。
2. 品牌、大区、战区按照上下级关系向下限制候选。
3. 省份、城市按照上下级关系向下限制候选。
4. 下级选择不自动填写上级。
5. 上级重新选择时不被当前下级锁死。
6. 上级变化只清除冲突的下级，不清除跨维度条件。
7. 大区、战区不限制省份、城市候选。
8. 省份、城市不限制大区、战区候选。
9. 巡回员、治理员只与门店双向缩小候选。
10. 巡回员、治理员不限制组织和地域候选。
11. 组织和地域不限制巡回员、治理员候选。
12. 门店候选正确反映当前所有条件的交集。
13. 销售顾问筛选只返回该顾问本人数据。
14. 同字段多选采用 OR，不同字段采用 AND。
15. 跨维度没有共同数据时显示 0 条，已选条件继续保留。
16. 多选人员中部分人员无结果时不自动清除。
17. 人员姓名搜索遇到重名时可以区分并选择具体人员。
18. 所有筛选、关系及查询均使用唯一 ID。
19. 人员与门店关系按数据发生时间判断。
20. 无权限数据不出现在候选项、搜索结果或最终列表中。

## 十二、最终记忆口径

> 组织管组织，地域管地域，人员只直接联动门店；门店承接各条件交集，所有条件最终共同筛选列表。
