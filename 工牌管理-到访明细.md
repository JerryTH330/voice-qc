# 工牌管理到访明细

## 1. 页面目标

到访明细是完整到访台账，用于逐条查看到访记录与录音的匹配、上传、转写和分析进度。页面不再承担门店异常监控；门店汇总、匹配率和原因分布统一在“录音排查”查看。

从录音排查带条件进入时，页面顶部显示“返回录音排查”；直接从侧边栏进入到访明细时不显示该按钮。

页面中的“业务 ID”取自销售助手返回的 `businessId` 字段，用于追踪到访记录；录音处理以录音 ID 追踪。

页面支持通过固定 Excel 模板手动补传到访明细。上传入口支持选择或拖拽 `.xlsx`、`.xls` 文件，单个文件不超过 10 MB；选择文件后才能开始上传并进入校验。

### 1.1 录音匹配、合并与裁剪

到访和试驾场景统一以客流记录的开始时间、结束时间作为录音有效范围。处理规则如下：

1. 先找出与客流时间范围存在交集的已上传录音片段。
2. 按录音实际时间顺序合并这些片段。
3. 合并后只保留“客流时间范围”和“工牌实际录音时间范围”的交集，不补造工牌未录到的内容。

示例：

- 客流时间为 `09:01—09:58`，工牌实际录音时间为 `09:00—10:00`，最终按客流时间裁剪为 `09:01—09:58`。
- 客流时间为 `09:01—09:58`，工牌实际录音时间为 `09:20—10:00`，最终取二者交集，裁剪为 `09:20—09:58`。

每个录音片段都有唯一 ID。系统必须按片段 ID 去重，同一个片段不会因重复回调或重复处理被重复合并、重复计数或重复分析。

同一客流所需片段分批上传时，不等待全部片段到齐：只要已经上传至少一个片段，当天就先使用当前已上传片段完成一次匹配、合并、裁剪和分析，但该到访的匹配状态仍保持“录音上传中”。后续有新的片段上传后，使用该客流当前已上传的完整片段集合重新执行录音合并、裁剪和分析；新的分析结果作为当前结果，旧结果不得与新结果重复统计。若该客流对应的所有片段均未上传，则当前没有可用于合并、裁剪和分析的音频，匹配状态同样为“录音上传中”。

### 1.2 手动上传模板

模板固定包含以下 10 个必填字段，列名和顺序均不可修改：

1. 客户姓名
2. 客户电话
3. 到访日期
4. 开始时间
5. 结束时间
6. 顾问姓名
7. 所属门店
8. 门店编码
9. 业务场景
10. 意向车系

字段顺序按“客户信息 → 到访时间 → 接待员工与门店 → 业务信息”组织，便于人工连续填写；所属门店与门店编码相邻，上传校验时需检查二者是否一致。

## 2. 筛选条件

筛选顺序固定为：组织／门店、到访来源、匹配状态、时间范围、精确查询、操作。

- 时间范围必须放在匹配状态后面。
- 业务 ID、录音 ID、客户 ID、客户姓名、客户电话、顾问姓名六个输入框收入“更多查询”，默认收起，展开后保持原有独立输入能力。
- 多个输入框有值时，所有输入条件同时生效。
- 匹配状态保留“全部未匹配”和“无录音、匹配失败”组合查询项，并保留“全部状态”和各具体状态。“无录音、匹配失败”对应异常需处理口径；处理中只作为结果摘要口径，不重复增加到状态下拉框。
- 所有筛选条件同时生效，点击“重置”恢复默认值。

## 3. 结果摘要

页面删除与录音排查重复的五张指标卡，在列表标题下使用一行结果摘要展示：到访数、已匹配、处理中、异常需处理。

四类数量统一按业务 ID 去重，同一业务 ID 只计数一次。“异常需处理”为匹配失败与无录音的合计，不改变明细中的原始状态和具体原因。

| 指标 | 统计口径 |
| --- | --- |
| 到访数 | 当前筛选条件下去重后的业务 ID 数 |
| 已匹配 | 当前最新状态为“已匹配”的去重业务 ID 数 |
| 处理中 | 当前最新状态为“待匹配、录音上传中、录音转写中、模型分析中”的去重业务 ID 数 |
| 异常需处理 | 当前最新状态为“匹配失败”或“无录音”的去重业务 ID 数 |

明细列表仍保留待匹配、录音上传中、录音转写中和模型分析中四个原始状态，便于定位具体处理环节。

“客户不同意录音”不再作为独立匹配状态，也不出现在状态筛选中；对应到访归入“无录音”，不同意录音只作为未采集录音的具体原因保留。

同一个业务 ID 关联多条录音时，以该到访的当前汇总状态为准，只计数一次。已上传片段不足但至少已有一个片段时，仍先完成当次匹配和分析，但汇总状态保持“录音上传中”；所有片段均未上传时也归入“录音上传中”，但没有可生成的分析结果。后续新片段到达后，使用该到访当前已上传的完整片段集合重新处理。页面统计只使用当前汇总状态，不同时计入旧结果和新结果。

## 4. 列表字段

列表不展示“录音 ID”，录音 ID 仍保留为查询条件，便于已知录音 ID 时反查到访记录。

主列表必须保留完整 17 列，不合并、不隐藏、不收缩为简版列表。

列表字段固定按以下顺序展示：到访日期、开始时间、结束时间、匹配状态、匹配详情、客户姓名、客户电话、顾问姓名、所属门店、业务场景、意向车系、匹配完成时间、到访来源、数据更新时间、客户 ID、门店编码、业务 ID。

排序逻辑为“到访时间 → 匹配结果 → 客户与接待信息 → 系统追溯字段”。业务 ID 对应销售助手的 `businessId` 字段。

分页必须按筛选后的真实记录数计算，默认每页 20 条，可切换为 50 条；支持上一页、下一页、页码和指定页跳转。筛选、重置或从录音排查下钻时返回第 1 页。

## 5. 状态定义

| 页面状态 | 页面口径 |
| --- | --- |
| 已匹配 | 到访与录音已经建立关系，且整个处理流程最终成功 |
| 录音上传中 | 该客流对应的录音片段尚未全部上传，包括“已上传部分片段、仍有片段未上传”和“所有片段均未上传”两种情况；部分片段已经完成合并、裁剪和分析时，状态仍保持录音上传中 |
| 录音转写中 | 录音已上传，ASR 转写尚未完成 |
| 模型分析中 | ASR 转写已完成，模型分析尚未完成 |
| 匹配失败 | 到访与录音匹配流程已经结束但未成功，具体原因只包括未绑定工牌、到访开始日期和结束日期非同一天 |
| 无录音 | 到访时间段内没有可用于后续处理的录音，具体原因单独展示 |
| 待匹配 | 到访记录已经接收，尚未完成录音匹配 |

“录音上传中”和“无录音”不能只按当前已上传片段数区分：当前为 0 个已上传片段，但仍有应上传片段尚未到达时，状态是“录音上传中”；只有确认到访时段内没有产生可用录音，而不是仍在等待上传时，才归入“无录音”。

### 5.1 匹配失败详情

点击“匹配失败”的匹配详情时，打开该销售顾问对应的录音上传明细，查询日期固定使用该条记录的到访日期，不使用结束日期。

- 失败原因为“未绑定工牌”时，录音上传明细显示“未匹配到录音，请绑定工牌”。
- 失败原因为“到访和结束日期非同一天”时，录音上传明细显示“未匹配到录音，请确认并更新到访日期”。

## 6. 循环与明略接口核对结论

两份接口资料都没有直接提供一套可完整映射上述页面状态的统一状态字段。这些状态属于质检系统的处理状态，正式接入时需要由质检系统维护状态机和判定规则。

| 页面检查项 | 循环接口资料 | 明略接口资料 | 能力标记 |
| --- | --- | --- | --- |
| 筛选条件 | 可提供 `staff_id`、工牌 SN、日期等部分条件；不含完整组织、客户和到访来源 | 可提供工牌 SN 和日志时间；不含完整组织、客户和到访来源 | 需要结合其他系统数据 |
| 结果摘要 | 没有页面处理状态及对应统计 | 没有页面处理状态及对应统计 | 需要结合其他系统数据 |
| 列表“录音 ID” | 文档有客户意向回调 `bid/source_id`，但未说明其等同于质检录音 ID | 原始音频回调有 `object_key`，音频处理回调有 `group_key/task_id`，但未定义统一的质检录音 ID | 需要结合其他系统数据 |
| 匹配详情 | 只有按天录音时长和已上传时长，不能直接返回匹配详情 | 待上传列表、操作日志、录音回调可作为上传和设备事件证据 | 需要结合其他系统数据 |
| 已匹配、匹配失败、待匹配 | 未定义 | 未定义 | 当前接口无法获得 |
| 录音上传中 | 仅有已上传录音时长，没有“上传中”状态定义 | `reclist/recnu` 可反映待上传录音，操作日志有上传、成功、失败事件，但没有页面“录音上传中”的统一状态定义 | 可通过接口字段计算 |
| 录音转写中 | 未定义 | ASR 回调示例只展示完成回调和 `status=100`，没有“转写中”的枚举定义 | 当前接口无法获得 |
| 模型分析中 | 未定义 | 未定义页面所指模型分析流程 | 当前接口无法获得 |
| 无录音 | 没有直接状态；可结合按天录音时长辅助判断，但不能证明到访时段内无录音 | 可结合开关机、录音事件及待上传列表判断，但仍需到访起止时间 | 需要结合其他系统数据 |
| 分页 | 按天统计接口支持分页 | 获取设备日志接口支持分页 | 接口直接提供 |
| 页面操作 | 未定义 | 可提供部分日志和回调证据 | 需要结合其他系统数据 |

### 6.1 原始资料中的明确内容

- 循环接口明确提供：按日的总录音时长、系统记录时长、已上传录音时长，以及员工当前绑定工牌的录音状态和已上传时长。
- 明略文档明确提供：`reclist` 待上传录音列表、上传操作日志、原始音频回调、ASR 完成回调、设备开关机和录音事件。
- 明略 ASR 示例中的 `status=100` 没有附枚举定义；上传日志示例中的 `status=6` 也没有附枚举定义，因此不能直接把这些值解释成页面状态。

## 7. 风险与待确认

- 同一业务 ID 下多条录音处于不同处理阶段时，上游或质检系统必须返回唯一的到访汇总状态；同一时点出现多个到访汇总状态应按数据异常处理，不得重复计入多个分类。
- 循环的 `bid/source_id`、明略的 `object_key/group_key/task_id` 与质检系统录音 ID 之间的映射关系尚未在两份资料中定义，需要接口联调时补充。
- 上传、转写、分析三个处理阶段需要质检系统给出明确进入条件、完成条件、失败条件和状态优先级。
- 系统需要明确“某次客流应有多少个录音片段”或等价的完整性判断依据，才能区分片段尚未上传完成与片段已经全部到齐；该判断不影响先对当前已上传片段进行匹配分析。

## 8. 录音处理验收规则

1. 客流时段完全包含在工牌录音时段内时，录音严格按客流开始、结束时间裁剪。
2. 客流时段与工牌录音时段部分重叠时，只保留二者交集。
3. 同一客流只上传了部分片段时，系统仍对当前已上传片段完成一次匹配、合并、裁剪和分析，但匹配状态保持“录音上传中”。
4. 新片段后续上传后，系统自动使用当前完整片段集合重新执行合并、裁剪和分析。
5. 同一片段 ID 不得重复合并、重复计数或重复分析；同一业务 ID 的旧、新分析结果不得重复进入页面统计。
6. 同一客流的所有片段均未上传时，匹配状态也为“录音上传中”，但不生成录音分析结果。
7. 当前已上传片段数为 0 不能直接判为“无录音”；必须先排除仍有片段等待上传的情况。
