# 工牌管理录音排查

## 1. 页面目标

录音排查是门店异常工作台，用于回答“哪家门店未匹配到访较多、具体原因是什么、应优先处理哪些记录”。页面只按门店汇总，不提供员工汇总维度；顾问信息在未匹配明细中查看。

录音排查与到访明细必须共用同一份去重到访数据、当前汇总状态和具体原因，不允许各自维护独立汇总数据。

## 2. 筛选条件

筛选顺序固定为：品牌、大区、战区、门店、时间范围、操作。所有条件同时生效；重置后恢复全部门店的默认范围。

## 3. 顶部指标

| 指标 | 统计口径 |
| --- | --- |
| 到访数 | 当前筛选条件下去重业务 ID 数 |
| 已匹配到访 | 当前汇总状态为已匹配的到访数 |
| 录音匹配率 | 已匹配到访数 ÷ 到访数 |
| 处理中 | 待匹配、录音上传中、录音转写中、模型分析中的到访数 |
| 异常需处理 | 匹配失败和无录音的到访数 |

到访数必须等于“已匹配到访 + 处理中 + 异常需处理”。录音片段数不用于计算本页匹配率。处理中只表示流程尚未结束，不属于异常；异常需处理只包含无录音和匹配失败。

## 4. 门店汇总

列表字段固定为：门店、到访数、已匹配到访、处理中、异常需处理、录音匹配率、操作。门店列内同时展示大区和战区，不再拆成多个独立宽列。

默认按异常需处理数从高到低排序；异常需处理数相同时，按录音匹配率从低到高排序。页面不提供“门店汇总／未匹配明细”切换 Tab。

## 5. 下钻到访明细

录音排查不再维护独立的逐条明细列表，所有下钻统一进入“到访明细”页面：

- 点击操作列“查看异常需处理”，带入当前时间范围、当前门店和“异常需处理”，只展示无录音、匹配失败记录。
- 从录音排查下钻后，到访明细页顶部显示“返回录音排查”，返回后保留原录音排查筛选条件。
- 到访明细中的匹配详情继续复用录音上传或工牌事件证据。

## 6. 补传片段后的汇总更新

- 同一到访只上传了部分录音片段时，先按当前已上传片段完成匹配分析，但该到访的汇总状态仍为“录音上传中”，并计入“处理中”。
- 同一到访的所有录音片段均未上传时，汇总状态同样为“录音上传中”并计入“处理中”，但当前没有录音分析结果。
- 后续新片段上传后，系统使用该到访当前已上传的完整片段集合重新合并、裁剪和分析，并刷新门店汇总和匹配率。
- 每个录音片段按唯一 ID 去重；补传和重新分析不会新增一条到访，也不会让同一业务 ID 在旧、新结果中重复计数。
