# 工牌管理通用规则

## 1. 适用范围

本文档统一“工牌总览”“工牌明细”“工牌事件”“到访明细”和“录音排查”共用的数据来源、统计口径和异常处理规则，作为产品设计、接口开发、联调和测试验收的共同依据。

工牌明细只展示“当前有效绑定关系”。历史换绑次数和已失效关系不进入本期绑定统计；未绑定工牌不进入工牌明细，但需要计入门店工牌资产数和工牌绑定率的分母。

## 2. 总体数据来源规则

### 2.1 组织与顾问数据

以下数据直接使用当前质检系统已经同步的数据，不要求循环接口或明略接口重复提供：

- 品牌
- 大区
- 战区
- 门店
- 顾问 ID
- 顾问姓名

组织和人员关系以当前质检系统为准。工牌与员工的绑定关系来自循环智能，循环智能返回的 `staff_id` 与当前质检系统的顾问 ID 是同一标识，不需要额外的 ID 映射表。页面按以下链路补齐人员和组织信息：

`工牌 SN → 循环智能 staff_id（= 顾问 ID）→ 顾问主数据 → 门店 → 战区 → 大区 → 品牌`

`staff_id` 无法匹配质检系统顾问主数据时，按数据同步异常处理，不自动猜测顾问、门店或组织信息。

### 2.2 工牌绑定关系

- 当前有效绑定关系使用循环智能返回的“`staff_id—工牌 SN`”关系。
- 循环智能的 `staff_id` 直接作为质检系统顾问 ID，一条当前有效关系定义为：一个 `staff_id` 当前绑定一枚工牌 SN。
- 门店、战区、大区、品牌和顾问姓名均通过 `staff_id` 关联质检系统顾问主数据取得，不要求工牌接口重复提供。
- 页面中的“工牌绑定数”统一指“当前有效绑定关系数”，不表示累计绑定次数或历史换绑次数。
- 未绑定员工、未绑定工牌、已经失效的历史关系不进入当前有效绑定统计。
- 同一员工同时绑定多枚工牌时，按多条当前有效关系统计；“绑定工牌员工数”按员工去重。

### 2.3 门店工牌资产数据

- 门店工牌资产数需要使用能够覆盖已绑定和未绑定工牌的完整资产清单。
- 当前循环接口和明略接口资料均未确认能直接提供完整门店资产清单，因此权威数据来源仍待确认。
- 不允许用当前有效绑定关系反推门店工牌资产数，否则会遗漏未绑定工牌并导致绑定率虚高。
- 正式数据至少需要提供工牌 SN、当前所属门店、有效资产状态和更新时间，并能识别报废、退役及跨店调拨。

### 2.4 工牌类型与录音上传周期

工牌来源、硬件版本和常规录音上传周期按工牌 SN 前缀识别：

| 工牌 SN 前缀 | 供应方 | 硬件版本 | 常规录音上传周期 |
| --- | --- | --- | --- |
| `MLR` | 明略 | 4G 版本工牌 | 每 5 分钟上传一次至云端 |
| `RAGPW` | 循环智能 | 充电坞版本老工牌 | 每 30 分钟上传一次至云端 |

上述上传周期用于判断录音片段可能的到达节奏。同一到访所需录音片段可以分批到达，不能因为首批片段数量不足就终止本次到访的匹配和分析。

### 2.5 设备状态数据优先级

同一项设备状态在明略接口和循环接口中都能获得时，默认优先使用明略数据，循环数据作为补充或异常核对依据。

| 数据项 | 首选来源 | 补充来源 | 规则 |
| --- | --- | --- | --- |
| 设备在线状态 | 明略 | 循环 WiFi 活跃状态 | 明略心跳 5 分钟内有效时视为在线 |
| 录音状态 | 明略 | 循环 | 明略 `record_status` 优先 |
| 剩余电量 | 明略 | 循环 | 明略 `remain_power` 优先；页面只展示原始电量值，不按数值设置颜色或推断低电量预警 |
| 工牌电量预警事件 | 明略 | 无 | 仅当操作日志出现 `ops=system`、`act=battery low` 时认定有预警；工牌明细列表不展示该字段 |
| 剩余内存 | 明略 | 循环 | 明略按剩余空间占比计算；循环 `memory` 可直接作为剩余百分比 |
| 待上传录音数 | 明略 | 循环按录音时长差异辅助核对 | 明略 `recnu` 作为页面数量 |
| 信号强度 | 明略 | 无 | 使用 `rssi` 转换为页面等级 |
| 最新数据同步时间 | 明略 | 循环 | 明略 `update_time` 优先，缺失时使用循环 `last_modify_ts` |
| 是否接入充电坞 | 循环 | 明略能力待确认 | 当前使用循环 `is_connected` |

### 2.6 状态时间与降级规则

- 明略设备状态以最近一次心跳或批量状态查询结果为准。
- 明略心跳超过 5 分钟未更新时，设备按离线处理。
- 某字段当前来源没有返回有效值时，页面显示“—”，不能用 0 代替未知值。
- 两个接口数据冲突时，页面按上表的首选来源展示，同时保留接口原始值和更新时间，便于问题核查。
- 所有时间点、时间范围和时长统一精确到时分秒；时间点统一展示为 `YYYY-MM-DD HH:mm:ss`。

## 3. 通用验收规则

1. 工牌绑定数始终表示当前有效绑定关系数，不是历史绑定次数。
2. 品牌、大区、战区、门店、顾问 ID、顾问姓名均来自当前质检系统。
3. 两个接口都有的设备状态，页面默认采用明略值；“是否接入充电坞”按本文件的数据优先级执行。
4. 字段缺失与数值 0 必须区分，未知值不得显示为 0。
5. 筛选、汇总、列表总数和分页总数必须使用同一数据范围和同一统计口径。
6. 所有页面的时间点、时间范围和时长必须精确到时分秒。
7. 循环智能返回的 `staff_id` 必须直接按质检系统顾问 ID 关联人员和组织；不得再建设或使用额外的员工 ID—顾问 ID 映射规则。
8. `staff_id` 无法命中顾问主数据时必须标记为数据异常，不得自动补猜门店、战区、大区或顾问信息。
9. 工牌供应方、硬件版本和常规录音上传周期必须按工牌 SN 前缀识别：`MLR` 对应明略 4G 工牌和 5 分钟周期，`RAGPW` 对应循环智能充电坞版本老工牌和 30 分钟周期。
