# 交接日志

## 当前接手摘要
- 用户最新要求：先把 `filter-regression-test.js` 跑通，再随主仓库一起提交。
- 已定位到失败根因是浏览器回归脚本等待过早，不是页面筛选逻辑本身缺失；脚本现已改为等待页面 `load` 完成，并显式等待“数据来源”筛选挂载后再校验。
- 重新运行 `node filter-regression-test.js` 已通过，确认数据来源切换后的默认场景和指标变化能正常校验。
- `AI画像` 和 `意向特征热力图` 两个区域的高度已保持一致。
- 当前主要风险不是脚本逻辑，而是页面里原有的部分本地图片/外链字体资源在 `file://` 环境下会加载失败，但不影响这次筛选功能。

## 最近 5 次工作记录
1. 2026-05-18：修复回归脚本抢跑
   - 用户想做什么：`客户洞察` 自带的浏览器回归脚本要先跑通，再继续提交。
   - 已完成什么：定位到脚本使用 `waitUntil: "commit"` 加固定 `300ms` 等待过短，导致偶发在页面还没渲染出筛选按钮时就开始断言；已改成等待页面 `load` 完成，并显式等待 `[data-filter="source"]` 挂载后再继续。
   - 改动文件：`filter-regression-test.js`、`handoff-log.md`。
   - 验证：运行 `node filter-regression-test.js` 通过，输出 `allMetric=286`、`cloudMetric=286`、`badgeMetric=0`。
   - 待办或风险：页面里仍有一些 `file://` 环境下的静态资源加载失败日志，但不影响当前筛选和指标联动回归校验。

1. 2026-05-18：统一 AI画像 与 热力图 面板高度
   - 用户想做什么：让 `AI画像` 和 `意向特征热力图` 两个区域高度保持一致。
   - 已完成什么：在布局层给 `insight-layout` 增加拉伸对齐；给两个面板补 `height: 100%`；移除了右侧 `data-panel` 单独的 `margin-top: 22px`，避免它比左侧更低、更短。
   - 改动文件：`客户洞察.css`、`handoff-log.md`。
   - 验证：本地用浏览器脚本读取 `.portrait-panel` 和 `.data-panel` 的 `getBoundingClientRect()`，确认 `top` 和 `height` 完全一致。
   - 待办或风险：如果后续改动两个面板的标题区内容高度，仍建议继续保留这套网格拉伸方式，不要再单独给其中一个面板加外边距。

1. 2026-05-18：把不可选场景改为置灰展示
   - 用户想做什么：不是把其他场景隐藏掉，而是要继续展示，只是置灰且不能选。
   - 已完成什么：把质检场景重新改成始终展示全量按钮；在 `云外呼/数字工牌` 下，对不允许的场景加禁用态和置灰样式；更新回归脚本，增加“按钮仍展示但禁用状态正确”的校验。
   - 改动文件：`客户洞察.js`、`filter-regression-test.js`、`handoff-log.md`。
   - 验证：运行 `node filter-regression-test.js` 通过，确认所有场景都会展示，且 `云外呼/数字工牌` 下只有对应场景可选，其他场景为禁用状态。
   - 待办或风险：需求口径这几轮有多次变化，后续如果再调整，建议先明确“展示”“默认选中”“可点击”这三件事分别怎么定义。

1. 2026-05-18：按数据来源限制质检场景可选项
   - 用户想做什么：`云外呼` 只能选“首触跟进/邀约进店/排程确认”，`数字工牌` 只能选“门店接待/试乘试驾”，其他场景不可选。
   - 已完成什么：把质检场景的渲染逻辑改成按数据来源裁剪；同时在校验逻辑里只保留当前来源允许的场景，避免残留旧选项；更新回归脚本，增加“可选场景数量和内容”的校验。
   - 改动文件：`客户洞察.js`、`filter-regression-test.js`、`handoff-log.md`。
   - 验证：运行 `node filter-regression-test.js` 通过，确认 `云外呼` 下只显示 3 个对应场景，`数字工牌` 下只显示 2 个对应场景，且数据联动仍正常。
   - 待办或风险：这次规则与之前“场景始终全部展示”的需求相反，后续如果再改展示方式，需要先确认最终产品口径。

1. 2026-05-18：微调录音覆盖率说明文案
   - 用户想做什么：把“至少命中1条录音的线索数/全部线索数”改成更短的“至少命中1条录音的线索数/线索数”。
   - 已完成什么：先把回归脚本目标改成新文案并跑出失败；再只修改卡片说明文案这一处；顺手把回归脚本的本地页面加载方式调稳，避免 `file://` 偶发超时。
   - 改动文件：`客户洞察.js`、`filter-regression-test.js`、`handoff-log.md`。
   - 验证：运行 `node filter-regression-test.js` 通过，确认录音覆盖率说明文案已改为“至少命中1条录音的线索数/线索数”，且原有筛选联动仍正常。
   - 待办或风险：应用内浏览器的直接自动核对被页面安全策略拦截，本次以本地回归脚本作为最终验证依据。

## 历史归档
- 2026-05-18：修正录音覆盖率口径：调整录音覆盖率计算逻辑，新增“coveredClueIds”模拟覆盖线索；把卡片说明改成“至少命中1条录音的线索数/全部线索数”；更新回归脚本并在浏览器复核卡片值和说明；验证 `node filter-regression-test.js` 通过，卡片显示 `22.6%`。
- 2026-05-18：修正业务场景展示方式：把业务场景按钮改成始终展示全量选项；保留数据来源驱动默认选中的逻辑；更新回归脚本，增加“业务场景展示完整”的校验；验证 `node filter-regression-test.js` 通过，确认 6 个业务场景按钮都会显示。
- 2026-05-18：调整客户洞察筛选联动：新增“数据来源”筛选，并让“质检场景”根据来源自动联动；顶部筛选变化后，下方数据都基于同一套筛选结果刷新；验证 `node filter-regression-test.js` 通过，指标会随筛选变化。
- 2026-05-14：准备 GitHub 推送：检查目录结构，确认没有现成 Git 仓库；创建交接日志；初始化 Git 仓库；设置 GitHub 远程地址；验证 `git remote -v`、`gh auth status`、`git init -b main`。
