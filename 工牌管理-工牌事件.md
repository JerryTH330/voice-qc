# 工牌管理：工牌事件

> 阅读本文件前，先读取 [工牌管理-通用规则.md](工牌管理-通用规则.md)。涉及接口能力或数据缺口时，再读取 [工牌管理-待确认事项.md](工牌管理-待确认事项.md)。

## 1. 页面目标

按单日和工牌 SN 还原设备开关机、录音、充电及低电量事件，并汇总当日开机时长和已上传录音时长。

## 2. 筛选条件

| 筛选项 | 明略接口依据 | 能力标记 | 页面规则 |
| --- | --- | --- | --- |
| 查询日期（单日） | 设备日志接口 `time` 创建时间范围 | 接口直接提供 | 以自然日的 `00:00:00—23:59:59` 查询 |
| 工牌 SN | 设备日志接口 `sn` | 接口直接提供 | 输入完整 SN 后查询；从工牌明细进入时自动带入 SN |
| 事件类型 | 操作日志 `ops + act`、系统日志状态变化 | 可通过接口字段计算 | 支持全部、开机、关机、开始/结束录音、开始/结束充电、低电量预警 |

SN 是明略设备日志接口的原生查询参数，正式接入时按完整 SN 精确查询。页面原型不再把 SN 与员工姓名混用为一个查询条件。

## 3. 总结信息

| 页面数据 | 明略接口依据 | 能力标记 | 规则 |
| --- | --- | --- | --- |
| 员工姓名 | 明略接口不提供 | 需要结合其他系统数据 | 根据工牌 SN 找到循环智能的有效绑定关系，再以 `staff_id（= 顾问 ID）` 关联质检系统顾问姓名 |
| 所属门店 | 明略接口不提供 | 需要结合其他系统数据 | 以循环智能绑定关系中的 `staff_id` 关联质检系统门店、战区、大区和品牌 |
| 工牌 SN | 设备日志 `sn` | 接口直接提供 | 展示当前查询 SN |
| 今日开机时长 | 操作日志 `system/start`、`system/shutdown` | 可通过接口字段计算 | 汇总当日各开机区间；缺失配对日志时显示“—”并标记异常 |
| 已上传录音时长 | 原始音频回调 `type=rec` 的 `duration`，以及文件名中的分片时长 | 可通过接口字段计算 | 每个录音片段都有唯一 ID，按片段 ID 去重后汇总当日已回调录音时长；不能使用 `rectd` 直接替代，`rectd` 是当日录音时长，不等于已上传时长 |

## 4. 事件数据

| 页面事件 | 明略接口依据 | 能力标记 | 规则 |
| --- | --- | --- | --- |
| 工牌开机 | 操作日志 `system/start` | 接口直接提供 | 使用日志 `tm` 作为事件时间 |
| 工牌关机 | 操作日志 `system/shutdown` | 接口直接提供 | 使用日志 `tm` 作为事件时间 |
| 开始录音 | 操作日志 `recording/start` 或 `recording/enable` | 接口直接提供 | 统一映射为“开始录音” |
| 结束录音 | 操作日志 `recording/stop` 或 `recording/disable` | 接口直接提供 | 统一映射为“结束录音” |
| 工牌电量预警 | 操作日志 `ops=system`、`act=battery low` | 接口直接提供 | 统一映射为“工牌电量预警”；不得通过剩余电量数值推断 |
| 开始充电 | 系统日志 `chg=cccv`，可结合 `usb/plugin` 核对 | 可通过接口字段计算 | 以充电状态由非充电变为 `cccv` 的时间为准；USB 插线不能单独等同于开始充电 |
| 结束充电 | 系统日志 `chg=finish` 或由充电变为 `charge-off` | 可通过接口字段计算 | 以充电状态结束的时间为准；`usb/plugout` 只表示拔线，不能单独等同于充电完成 |

上述范围内没有“当前接口无法获得”的设备事件；但员工姓名和所属门店必须通过循环智能工牌绑定关系中的 `staff_id（= 顾问 ID）` 关联当前质检系统的人员及组织数据。日志接口支持 `page`、`pageSize` 和 `pagination`，正式接入时可用于事件列表分页；当前原型事件量较少，暂按单日完整时间线展示。时间线使用单列布局，所有事件按事件时间从早到晚、自上而下排列；接口返回顺序不影响页面最终顺序。

### 4.1 事件层级

- 一级事件：工牌开机、工牌关机、开始充电、结束充电。使用主时间线样式展示。
- 二级事件：开始录音、结束录音、工牌电量预警。相对一级事件向右缩进，并使用更小的图标、字号和弱化背景展示。
- 事件层级只影响视觉展示，不改变事件时间排序、筛选和统计数量。

## 5. 页面验收重点

1. 页面支持“查询日期（单日）+ 完整工牌 SN”查询，SN 与员工姓名不得混用为同一输入项。
2. 从工牌明细点击“工牌事件”时，自动带入当前行的工牌 SN。
3. 页面固定覆盖工牌开机、工牌关机、开始录音、结束录音、开始充电、结束充电和工牌电量预警。
4. 今日开机时长根据开关机日志区间汇总，缺失配对日志时不得猜测。
5. 已上传录音时长按已收到的录音回调去重汇总，不得用当日录音时长 `rectd` 直接替代。
6. 开始充电、结束充电按 `chg` 状态变化判断；USB 插入、拔出只作为辅助证据。
7. 员工姓名和所属门店从当前质检系统的有效绑定及组织数据补齐，不能声称由明略接口直接提供。
8. 工牌事件使用单列时间线，按事件时间升序从上到下展示。
9. 开关机和充电事件按一级事件展示；录音开始、录音结束和低电量预警按二级事件展示，并有明确的缩进和视觉弱化。
10. 人员和组织信息必须通过循环智能绑定关系中的 `staff_id（= 顾问 ID）` 关联质检系统顾问主数据取得。
